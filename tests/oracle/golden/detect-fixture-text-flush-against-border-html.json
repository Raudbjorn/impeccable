{
  "stdout": "",
  "stderr": "\n<REPO>/tests/fixtures/antipatterns/flush-against-border.html\n  [label-line-height] 12px text with 2.00x line-height \"section: full border, padding \"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [cramped-padding] <section> \"flag-frameworks\": children flush against border+bg on right/bottom/left (no inset)\n    → Text is too close to the edge of its container. Two shapes: (1) an element with its own text where the padding is too low for the font size, and (2) a wrapper with text-bearing children and near-zero padding against a visible boundary (border, outline, or non-transparent background) — children land flush against the boundary line. Add at least 8px (ideally 12–16px) of padding inside bordered, outlined, or colored containers.\n  [label-line-height] 11px text with 2.18x line-height \"Supported dev servers\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"card: 1px border on all sides,\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [cramped-padding] <div> \"flag-card-borders\": children flush against border+bg on right/bottom/left (no inset)\n    → Text is too close to the edge of its container. Two shapes: (1) an element with its own text where the padding is too low for the font size, and (2) a wrapper with text-bearing children and near-zero padding against a visible boundary (border, outline, or non-transparent background) — children land flush against the boundary line. Add at least 8px (ideally 12–16px) of padding inside bordered, outlined, or colored containers.\n  [label-line-height] 12px text with 2.00x line-height \"no border, but a non-transpare\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [cramped-padding] <div> \"flag-bg-only\": children flush against bg on right/bottom/left (no inset)\n    → Text is too close to the edge of its container. Two shapes: (1) an element with its own text where the padding is too low for the font size, and (2) a wrapper with text-bearing children and near-zero padding against a visible boundary (border, outline, or non-transparent background) — children land flush against the boundary line. Add at least 8px (ideally 12–16px) of padding inside bordered, outlined, or colored containers.\n  [label-line-height] 12px text with 2.00x line-height \"outline draws outside the box;\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [cramped-padding] <div> \"flag-outline-only\": children flush against outline on right/bottom/left (no inset)\n    → Text is too close to the edge of its container. Two shapes: (1) an element with its own text where the padding is too low for the font size, and (2) a wrapper with text-bearing children and near-zero padding against a visible boundary (border, outline, or non-transparent background) — children land flush against the boundary line. Add at least 8px (ideally 12–16px) of padding inside bordered, outlined, or colored containers.\n  [label-line-height] 12px text with 2.00x line-height \"card: padding 12px 16px 12px 0\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [cramped-padding] <div> \"flag-asym-leftflush\": children flush against border+bg on left (no inset)\n    → Text is too close to the edge of its container. Two shapes: (1) an element with its own text where the padding is too low for the font size, and (2) a wrapper with text-bearing children and near-zero padding against a visible boundary (border, outline, or non-transparent background) — children land flush against the boundary line. Add at least 8px (ideally 12–16px) of padding inside bordered, outlined, or colored containers.\n  [label-line-height] 12px text with 2.00x line-height \"no bg, no border, no outline; \"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 11px text with 2.18x line-height \"Supported dev servers\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"border-top only, padding-top 2\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 11px text with 2.18x line-height \"Supported dev servers\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"card: 1px border + padding 18p\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"non-transparent bg + padding 1\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"outline + padding 14px 18px; c\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"card: padding 0, border all si\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"card: parent padding 0, but te\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"card: parent padding 0, but an\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"child padding 0, but it has th\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"marquee shell clips animated c\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [cramped-padding] <section> \"pass-marquee-shell\": children flush against bg on all sides (no inset)\n    → Text is too close to the edge of its container. Two shapes: (1) an element with its own text where the padding is too low for the font size, and (2) a wrapper with text-bearing children and near-zero padding against a visible boundary (border, outline, or non-transparent background) — children land flush against the boundary line. Add at least 8px (ideally 12–16px) of padding inside bordered, outlined, or colored containers.\n  [label-line-height] 12px text with 2.00x line-height \"outer shell has a scene backgr\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n\n25 anti-patterns found.\n",
  "exit": 2,
  "signal": null,
  "files": {}
}
