{
  "stdout": "",
  "stderr": "\n<REPO>/tests/fixtures/antipatterns/quality.html\n  [label-line-height] 12px text with 2.13x line-height \"no max-width on a paragraph\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"line-height: 1.0\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [tight-leading] line-height 1.00x (need >=1.3)\n    → Line height below 1.3x the font size makes multi-line text hard to read. Use 1.5 to 1.7 for body text so lines have room to breathe.\n  [label-line-height] 12px text with 2.13x line-height \"10px body text\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [tiny-text] 10px body text\n    → Body text below 12px is hard to read, especially on high-DPI screens. Use at least 14px for body content, 16px is ideal.\n  [label-line-height] 12px text with 2.13x line-height \"text-align: justify, no hyphen\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [justified-text] text-align: justify without hyphens: auto\n    → Justified text without hyphenation creates uneven word spacing (\"rivers of white\"). Use text-align: left for body text, or enable hyphens: auto if you must justify.\n  [label-line-height] 12px text with 2.13x line-height \"text-transform: uppercase on a\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [all-caps-body] text-transform: uppercase on 285 chars of body text\n    → Long passages in uppercase are hard to read. We recognize words by shape (ascenders and descenders), which all-caps removes. Reserve uppercase for short labels and headings.\n  [label-line-height] 12px text with 2.13x line-height \"letter-spacing: 0.15em on body\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [wide-tracking] letter-spacing: 0.15em on body text\n    → Letter spacing above 0.05em on body text disrupts natural character groupings and slows reading. Reserve wide tracking for short uppercase labels only.\n  [label-line-height] 12px text with 2.13x line-height \"h1 → h3 (missing h2)\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"max-width: 65ch on the paragra\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"line-height: 1.6\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"16px body text\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"text-align: justify + hyphens:\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"short label, uppercase\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"Featured\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"letter-spacing on a short uppe\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"Beta\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"h1 → h2 → h3\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.13x line-height \"terminal/diff metadata at tiny\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [skipped-heading] <h1> \"Top heading\" followed by <h3> \"Skips straight to h3\" (missing h2)\n    → Heading levels should not skip (e.g. h1 then h3 with no h2). Screen readers use heading hierarchy for navigation. Skipping levels breaks the document outline.\n\n23 anti-patterns found.\n",
  "exit": 2,
  "signal": null,
  "files": {}
}
