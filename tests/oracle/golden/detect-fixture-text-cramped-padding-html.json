{
  "stdout": "",
  "stderr": "\n<REPO>/tests/fixtures/antipatterns/cramped-padding.html\n  [low-contrast] 3.7:1 (need 4.5:1) — text #ffffff on #3b82f6\n    → Text does not meet WCAG AA contrast requirements (4.5:1 for body, 3:1 for large text). Increase the contrast between text and background.\n  [label-line-height] 12px text with 2.00x line-height \"14px body, 4px padding all sid\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"14px body, 2px padding all sid\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"16px body, 4px padding, backgr\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"1px vertical / 16px horizontal\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"12px vertical / 4px horizontal\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"24px heading, 8px padding (pas\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"32px hero, 6px vertical / 16px\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"<pre>, 2px padding, 14px monos\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \".detection-cmd replica: 13px f\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"small badge: 11px font, 4px / \"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"tag chip: 12px font, 4px / 10p\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"long inline code chip: compact\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"npx impeccable check\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"16px body, 8px padding (the or\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"14px body, 16px padding (gener\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"18px body, 12px padding (propo\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"multi-line <pre>, 8px padding,\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"multi-line <pre>, 12px padding\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"button: 14px font, 8px / 16px \"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"form input: 16px font, 8px / 1\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"24px heading, 16px padding\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n  [label-line-height] 12px text with 2.00x line-height \"32px hero, 20px / 32px padding\"\n    → UI labels, chips, badges and small text elements should not inherit large body-text line heights. Use a tighter line-height so spacing is controlled by padding.\n\n23 anti-patterns found.\n",
  "exit": 2,
  "signal": null,
  "files": {}
}
